// TODO: Write code to define and export the Employee class
class Employee{
        constructor (name, id, email) {
            this.name=name;
            console.log(name);
            this.id=id;
            console.log(id);
            this.email=email;
            console.log(email);
        } 
}